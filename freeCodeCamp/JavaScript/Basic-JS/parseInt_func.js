// Use parseInt() in the convertToInteger function so it converts the input string str into an integer, 
// and returns it.

function convertToInteger(str) {

    return parseInt(str);
}

console.log(convertToInteger("654"));
console.log(convertToInteger("46"));
console.log(convertToInteger("955745"));